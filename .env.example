# PostgreSQL Database URL
# For Docker, this should point to a PostgreSQL service.
# For local development, it could be a local PostgreSQL instance or SQLite.
DATABASE_URL="postgresql://user:password@db:5432/media_db"

# Redis URL
# The `docker-compose.yml` sets this up to connect to the 'redis' service.
REDIS_URL="redis://redis:6379/0"

# JWT Authentication Settings
# IMPORTANT: Change this to a long, random string in your actual .env file
SECRET_KEY="your_super_secret_key_goes_here"
ALGORITHM="HS256"
ACCESS_TOKEN_EXPIRE_MINUTES=30